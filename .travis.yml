language: python
python:
  - "2.7"
# command to install dependencies
install:
  - apt-get install libblas-dev liblapack-dev gfortran fluidsynth
  - travis_wait pip install -r requirements.txt
# command to run tests
script: nosetests
